/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.pexels.com",
      "pexels.com",
      "render.fineartamerica.com",
      "pixels.com",
    ],
  },
};

export default nextConfig;
